# Correct PostgreSQL URL format (add jdbc: prefix)
spring.datasource.url=jdbc:postgresql://dpg-d2138gbe5dus738edqeg-a/voting_db_hnoc?sslmode=require
spring.datasource.username=voting_db_hnoc_user
spring.datasource.password=30CXz4M43RmuqWkHDjbhuiB1tVR1WZSc

# Required driver class
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
# Explicit port binding for Render
server.port=${PORT:8080}
